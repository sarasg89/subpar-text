{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start:dev": "cd client && npm run dev",
    "start:prod": "cd client && npm run build && cd ../server && npm run start",
    "heroku-prebuild": "npm install --dev",
    "start": "npm run start:prod",
    "build": "cd client && npm run build",
    "install": "cd client && npm install && cd ../server && npm install"
  },
  "keywords": [],
  "author": "Sara Seoane Garcia",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}